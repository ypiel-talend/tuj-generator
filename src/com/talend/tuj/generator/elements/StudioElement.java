package com.talend.tuj.generator.elements;

import com.talend.tuj.generator.utils.Job;
import org.w3c.dom.Node;

public class StudioElement extends AbstractElement {

    public StudioElement(Node node, Job job) {
        super(node, job);
    }
}
